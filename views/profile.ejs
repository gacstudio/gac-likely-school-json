<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style/style.css" />
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@48,400,1,0" />
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <title>Likely School - <%= title %></title>
</head>
<body>
    <style>
        .user-generalities{
            display: flex;
            flex-direction: row;
            width: max-content;
            margin: max-content;
            float: left;
        }
        .user-generalities span{
            color: blue;
            text-shadow: 2px 2px 2px 2px rgba(0, 0, 0, .25);
        }
        .profile-container{
            background-color: red;
            position:absolute;
            top:50%;
            left:50%;
            transform: translate(-50%,-50%);
            width: 50vw;
            height: 50%;
            border-radius: 20px;
            padding:20px;
        }

        .profile-container button{
            padding-left:20px;
            padding-right:20px;
            padding-top:5px;
            padding-bottom:5px;
            margin-top: 10px;
            font-size: 20px;
            border-radius: 10px;
        }
        .functions{
            display: flex;
            flex-direction: column;
            width: max-content;
            float: right;
        }
        .info-container{
            float: left;
        }
        .general-info{
            display: flex;
            flex-direction: column;
        }
    </style>
    <div class="container">
        <header><%- include('./modules/header.ejs'); %></header>
        <main>
            <% 
                //var json = JSON.parse(JSON.stringify(user));
                //console.log("CLIENT: ",json);
            %>
            <div class="profile-container">
                <div class="user-info">
                    <div class="general-info">
                        <div class="user-generalities">
                            <p class="info-container">
                                <%= json.name %> <%= json.surname %>
                            </p>
                            <span class="material-symbols-outlined">
                                <%= (json.role == "ADMIN")? "verified_user": "person"%>
                            </span>
                        </div>
                        <p class="info-container">
                            Email: <%= json.email %>
                        </p>
                    </div>
                    <div class="functions">
                        <button>Chat</button>
                        <button>Meet</button>
                    </div>
                </div>

            </div>
        </main>
    </div>
    <script src="main.js"></script>
</body>
</html>